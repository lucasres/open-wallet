import { StyleSheet } from "react-native";

export default StyleSheet.create({
    input: {
        height: 40,
        borderWidth: 1,
        padding: 10,
        borderRadius: 6,
        backgroundColor: '#4A5859',
        marginBottom: 12,
        marginTop: 12,
    },
    container: {
        marginTop: 12,
    }
})